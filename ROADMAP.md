# anan-agent 下一阶段优化与拓展方案

> 生成时间：2026-09-18
> 状态说明：⬜ 未开始 / 🔶 进行中 / ✅ 完成
> 本文档为下一阶段 TODO 参照，完成后更新状态。

---

## 一、后端优化

### P0 — 影响核心链路可用性

- [ ] **1. 采集层联调（最大风险）**
  - 现状：`backend/app/clients/douyin.py` 的接口路径全是占位，从未和真实 TikTokDownloader 对接，整条"采集→下载→处理"链路实际跑不通，演示全靠种子数据
  - 方案：启动 TikTokDownloader 后对照 `/docs` 逐字段联调；封装层加响应字段适配器；加联调测试脚本
  - 文件：`backend/app/clients/douyin.py`

- [ ] **2. 任务队列**
  - 现状：FastAPI `BackgroundTasks` 进程内跑流水线，热榜 50 个视频瞬间并发 50 条流水线；进程重启任务全丢；无限速（风控硬伤）
  - 方案：Dramatiq/RQ + Redis（比 Celery 轻）；过渡方案：任务表 + APScheduler + 并发信号量，先做到串行化 + 可恢复 + 限速

- [ ] **3. pgvector 向量索引**
  - 现状：`corpus.embedding` 无索引，语料量大后检索退化为全表扫描
  - 方案：`CREATE INDEX ON corpus USING hnsw (embedding vector_cosine_ops);`

### P1 — 影响可维护性和数据质量

- [ ] **4. 流水线并发锁**
  - 现状：retry/批量采集可重复触发同一条视频并发处理
  - 方案：加 `processing` 状态或分布式锁

- [ ] **5. 数据库迁移机制**
  - 现状：`create_all` + 手写 sync_comments 脚本，表结构演进靠手工
  - 方案：引入 Alembic（requirements 已装）

- [ ] **6. 类别/模型变更后的重跑能力**
  - 现状：改类别定义、新增类别后老视频分类不更新；改 embedding 模型（dim 变化）corpus 不兼容
  - 方案：「批量重分类」「重建向量库」管理接口 + embedding 模型版本字段

- [ ] **7. 低置信度复核流程**
  - 现状：`CLASSIFY_CONFIDENCE_THRESHOLD=0.7` 配了没接进逻辑
  - 方案：`classified` 增加 `needs_review` 标记，视频库加"待复核"筛选

- [ ] **8. 会话与用户关联**
  - 现状：conversations 表无 user_id，加用户后历史会混
  - 方案：趁早加字段，成本最低

### P2 — 效果增强

- [ ] **9. RAG 混合检索**
  - 现状：只有向量相似度
  - 方案：向量 + `tsvector` 关键词混合 + rerank + 时间衰减（热点语料有时效）

- [ ] **10. 长会话历史压缩**
  - 现状：最近 20 条原文全塞进上下文，token 浪费
  - 方案：超过 N 轮后对早期历史做摘要压缩

- [ ] **11. 类别发现落地**
  - 现状：`backend/app/agents/discovery.py` 空实现
  - 方案：HDBSCAN 聚类 + LLM 命名 + 一键建类

---

## 二、前端优化

- [ ] **12. 视频详情抽屉（P1）**
  - 现状：列表只有元数据，OCR/ASR 文本、高赞评论、失败原因都看不到，调试分类质量等于盲调
  - 方案：加详情抽屉（后端详情接口已有 `GET /api/videos/{id}`）

- [ ] **13. 任务进度反馈（P1）**
  - 现状：采集提交后只能干等，5 秒轮询写死一次性
  - 方案：流水线传送带改定时轮询或 SSE 推送状态

- [ ] **14. 配置连接测试**
  - 现状：改完 LLM/Embedding 配置不知道通不通
  - 方案：配置页每个模型加"测试连接"按钮（后端加 ping 接口）

- [ ] **15. 对话体验细节**
  - 代码块语法高亮（markdown-it 配 highlight.js）
  - 历史会话重命名/搜索
  - 导出对话

- [ ] **16. 统一加载态**
  - 各页加骨架屏，加载体验一致

---

## 三、可拓展的新模块

- [ ] **① 内容再创作（最推荐）**
  - 价值：语料的终极用途——"用阴阳怪气风格写一条关于 XX 的评论"，类别人格 + RAG 一键生成同风格文案/视频脚本，采集数据的直接变现路径
  - 成本：低（现有 Agent 链路加个生成模式）

- [ ] **② 定时调度**
  - 每日热榜自动采集、盯达人更新，语料库自我增长
  - 依赖：任务队列（#2）

- [ ] **③ 数据看板（首页 Dashboard）**
  - 分类分布饼图、采集趋势、token 消耗、待复核数；现在首页直接跳采集页，缺全景视图
  - 成本：低（聚合查询 + ECharts）

- [ ] **④ 语料管理页**
  - 查看/编辑/删除 corpus、人工补充优质语料、改标后重建索引
  - 成本：低

- [ ] **⑤ 数据集导出**
  - 导出 JSONL（文本+类别标签），为后期微调小模型/LoRA 做准备
  - 成本：低

- [ ] **⑥ 智能体发布**
  - 把某个智能体发布成免登录分享链接或嵌入 widget
  - 成本：中

- [ ] **⑦ 评论舆情**
  - 评论情感倾向、热词统计，辅助选题
  - 成本：中

- [ ] **⑧ 视觉理解**
  - DeepSeek V4 视觉实验模型，对封面/关键帧做画面理解，补 ASR+OCR 之外的第三路信号
  - 成本：中

---

## 四、推荐路线

1. **先补地基**（顺序执行）：#1 采集联调 → #3 向量索引 → #5 Alembic → #2 任务队列
2. **再补体验**：#12 视频详情抽屉 + #14 连接测试 + #7 待复核流程
3. **然后做变现**：① 内容再创作 + ③ 数据看板（做完项目从"玩具"变"工具"）
4. **最后做规模**：② 定时调度、#9 混合检索、#11 类别发现
